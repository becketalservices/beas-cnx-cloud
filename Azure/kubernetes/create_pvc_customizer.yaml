apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: customizernfsclaim
  namespace: connections
spec:
  accessModes:
  - ReadOnlyMany
  resources:
    requests:
      storage: 8Gi
  storageClassName: azurefile

